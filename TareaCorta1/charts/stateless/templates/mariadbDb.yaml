{{ if .Values.mariadb.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.config.mariadbDb.mapName }}
  namespace: {{ .Values.namespace }}
data:
{{ (.Files.Glob "sql/babynames.sql").AsConfig | indent 2 }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Values.config.mariadbDb.name }}
spec:
  template:
    spec:
      volumes:
      - name: {{ .Values.config.mariadbDb.volumeName }}
        configMap:
          name: {{ .Values.config.mariadbDb.mapName }}
          defaultMode: 0777
      containers:
      - name: pi
        image: {{ .Values.config.mariadbDb.image }}
        volumeMounts:
        - name: {{ .Values.config.mariadbDb.volumeName }}
          mountPath: /scripts
        env:
          - name: MARIADBHOST
            value: "databases-mariadb-master-node"
          - name: MARIADBPASS
            valueFrom:
              secretKeyRef:
                name: databases-mariadb
                key: mariadb-root-password
                optional: false
          - name: MARIADB_DATABASE
            value: babynames
        command: ["/bin/sh"]
        args: ["-c", "mysql -p$(MARIADBPASS) -uroot -h$(MARIADBHOST) < /scripts/babynames.sql; mysql -p$(MARIADBPASS) -uroot -h$(MARIADBHOST) -e \"SHOW DATABASES\""]
      restartPolicy: Never
  backoffLimit: 7

{{ end }}